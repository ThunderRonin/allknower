[install]
# Use exact versions by default
exact = false

[install.cache]
# Cache directory — use $HOME; bun does not expand ~ in bunfig strings
dir = "/Users/allmaker/.bun/install/cache"

[run]
# Bun shell
shell = "bun"
